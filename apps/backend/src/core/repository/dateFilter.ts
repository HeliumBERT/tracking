export function dateRangeFilter(fromDate?: Date, toDate?: Date): undefined {
    // let query: { gte?: Date; lte?: Date } | undefined = undefined;

    // if (fromDate !== undefined || toDate !== undefined) {
    //     query = {};
    //     if (fromDate) query.gte = new Date(fromDate);
    //     if (toDate) query.lte = new Date(toDate);
    // }

    // return query;

    return undefined;
}